---
phase: 02-stripe-integration-testing-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/shared/core/services/webhook-events.service.ts
  - app/api/superadmin/webhooks/route.ts
  - app/api/superadmin/webhooks/replay/route.ts
  - scripts/replay-webhook-event.ts
  - tests/unit/stripe/webhook-events.service.test.ts
  - tests/integration/stripe/webhook-replay-flow.test.ts
autonomous: true
requirements: [RESIL-02]

must_haves:
  truths:
    - "Superadmin ou script consegue reprocessar eventos Stripe com falha sem duplicar efeitos colaterais"
    - "Eventos replayed ficam rastreaveis com status atualizado em webhook_events"
    - "Replay tem validacao de entrada e respostas HTTP claras"
  artifacts:
    - path: "app/shared/core/services/webhook-events.service.ts"
      provides: "Listagem e replay transacional de eventos webhook"
      exports: ["listWebhookEvents", "replayWebhookEvent"]
    - path: "app/api/superadmin/webhooks/replay/route.ts"
      provides: "Endpoint POST para replay de evento com falha"
      exports: ["POST"]
    - path: "scripts/replay-webhook-event.ts"
      provides: "CLI para replay operacional"
      contains: "--event"
  key_links:
    - from: "app/api/superadmin/webhooks/replay/route.ts"
      to: "app/shared/core/services/webhook-events.service.ts"
      via: "import replayWebhookEvent"
      pattern: "replayWebhookEvent"
    - from: "scripts/replay-webhook-event.ts"
      to: "app/shared/core/services/webhook-events.service.ts"
      via: "service call"
      pattern: "replayWebhookEvent"
---

<objective>
Entregar backend de replay e rastreabilidade de webhooks para fechar RESIL-02.

Purpose: Sem replay operacional, eventos falhos exigem intervencao manual no banco e nao ha recuperacao segura.
Output: Service de webhook events + API de replay + script CLI + cobertura de testes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-webhook-hardening-foundation/01-02-SUMMARY.md
@.planning/phases/02-stripe-integration-testing-observability/02-RESEARCH.md

@app/api/webhooks/stripe/route.ts
@app/api/superadmin/faturas/route.ts
@app/shared/core/services/billing.service.ts
@app/shared/core/services/logger.service.ts
@supabase/migrations/20260324120000_create_webhook_events.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar service de webhook events com replay seguro</name>
  <files>app/shared/core/services/webhook-events.service.ts, tests/unit/stripe/webhook-events.service.test.ts</files>
  <read_first>
    - app/api/webhooks/stripe/route.ts
    - app/shared/core/services/billing.service.ts
    - app/shared/core/services/logger.service.ts
    - supabase/migrations/20260324120000_create_webhook_events.sql
  </read_first>
  <behavior>
    - Teste: `replayWebhookEvent(eventId)` rejeita evento nao encontrado com erro de dominio claro.
    - Teste: `replayWebhookEvent(eventId)` processa apenas eventos com `status=failed` e atualiza para `processed`.
    - Teste: `listWebhookEvents(filters)` retorna payload resumido e ordenacao por `created_at` desc.
  </behavior>
  <action>Implementar `listWebhookEvents` e `replayWebhookEvent` usando `getDatabaseClient()`. `replayWebhookEvent` deve: validar `stripe_event_id`, carregar payload salvo, reconstruir objeto minimo do evento, chamar `syncStripeSubscriptionToLocal` quando houver subscription id, atualizar `status`, `processing_error`, `processed_at`, `processing_time_ms`, e logar via `logger.info/error` com contexto `superadmin-webhooks`.</action>
  <verify>
    <automated>npx jest tests/unit/stripe/webhook-events.service.test.ts --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `app/shared/core/services/webhook-events.service.ts` exporta `listWebhookEvents` e `replayWebhookEvent`.
    - `replayWebhookEvent` contem update explicito para `status: "processed"` e `status: "failed"`.
    - `tests/unit/stripe/webhook-events.service.test.ts` possui casos para evento inexistente e replay de falha.
  </acceptance_criteria>
  <done>Service de replay implementado com testes unitarios verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expor replay/listagem em API superadmin e script operacional</name>
  <files>app/api/superadmin/webhooks/route.ts, app/api/superadmin/webhooks/replay/route.ts, scripts/replay-webhook-event.ts, tests/integration/stripe/webhook-replay-flow.test.ts</files>
  <read_first>
    - app/api/superadmin/faturas/route.ts
    - app/api/superadmin/metricas/route.ts
    - app/shared/core/services/superadmin-auth.service.ts
    - app/shared/core/services/webhook-events.service.ts
  </read_first>
  <behavior>
    - Teste: `GET /api/superadmin/webhooks` sem auth retorna 401.
    - Teste: `POST /api/superadmin/webhooks/replay` com body invalido retorna 400 com `details`.
    - Teste: replay bem-sucedido retorna 200 com `{ replayed: true }`.
  </behavior>
  <action>Criar `GET /api/superadmin/webhooks` com filtros `status`, `limit`, `starting_after` usando Zod e `requireSuperadminForAPI`. Criar `POST /api/superadmin/webhooks/replay` com schema `{ eventId: string }` e chamada ao `replayWebhookEvent`. Criar script `scripts/replay-webhook-event.ts` aceitando `--event <stripe_event_id>`, imprimindo resultado e exit code 1 em falha.</action>
  <verify>
    <automated>npx jest tests/integration/stripe/webhook-replay-flow.test.ts --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `app/api/superadmin/webhooks/route.ts` exporta `GET` e usa `requireSuperadminForAPI`.
    - `app/api/superadmin/webhooks/replay/route.ts` exporta `POST` e valida body com Zod.
    - `scripts/replay-webhook-event.ts` contem parser de `--event`.
    - Teste de integracao cobre 401, 400 e 200 para replay.
  </acceptance_criteria>
  <done>APIs e script CLI de replay funcionando com testes de integracao passando.</done>
</task>

</tasks>

<verification>
Executar `npx jest tests/unit/stripe/webhook-events.service.test.ts tests/integration/stripe/webhook-replay-flow.test.ts --no-coverage` e confirmar 100% verde.
</verification>

<success_criteria>
- Replay de webhook com falha funciona via API e script.
- Eventos replayed ficam rastreaveis no banco com status atualizado.
- Contratos de erro/sucesso das rotas ficam testados.
</success_criteria>

<output>
After completion, create `.planning/phases/02-stripe-integration-testing-observability/02-01-SUMMARY.md`
</output>
