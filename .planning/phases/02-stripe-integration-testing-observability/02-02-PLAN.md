---
phase: 02-stripe-integration-testing-observability
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/superadmin/(dashboard)/webhooks/page.tsx
  - app/superadmin/(dashboard)/webhooks/components/webhook-events-list.tsx
  - app/superadmin/(dashboard)/components/superadmin-sidebar.tsx
  - tests/unit/stripe/webhook-log-ui.test.tsx
autonomous: true
requirements: [RESIL-03]

must_haves:
  truths:
    - "Superadmin consegue visualizar eventos de webhook recentes com status e payload resumido"
    - "Superadmin consegue acionar replay de evento failed pela interface"
    - "Tela indica loading, erro e estado vazio de forma explicita"
  artifacts:
    - path: "app/superadmin/(dashboard)/webhooks/page.tsx"
      provides: "Pagina de observabilidade de webhook"
    - path: "app/superadmin/(dashboard)/webhooks/components/webhook-events-list.tsx"
      provides: "Tabela/listagem com filtros e acao de replay"
    - path: "app/superadmin/(dashboard)/components/superadmin-sidebar.tsx"
      provides: "Entrada de navegacao para Webhooks"
  key_links:
    - from: "app/superadmin/(dashboard)/webhooks/components/webhook-events-list.tsx"
      to: "app/api/superadmin/webhooks/route.ts"
      via: "fetch GET"
      pattern: "/api/superadmin/webhooks"
    - from: "app/superadmin/(dashboard)/webhooks/components/webhook-events-list.tsx"
      to: "app/api/superadmin/webhooks/replay/route.ts"
      via: "fetch POST"
      pattern: "/api/superadmin/webhooks/replay"
---

<objective>
Entregar observabilidade de webhook no superadmin com acao de replay na UI para fechar RESIL-03.

Purpose: A operacao precisa enxergar rapidamente eventos falhos e corrigir sem acesso direto ao banco.
Output: Nova pagina Webhooks no dashboard superadmin + componente de listagem/replay + teste de UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-stripe-integration-testing-observability/02-01-SUMMARY.md

@app/superadmin/(dashboard)/faturas/page.tsx
@app/superadmin/(dashboard)/faturas/components/invoice-list.tsx
@app/superadmin/(dashboard)/components/superadmin-sidebar.tsx
@app/api/superadmin/webhooks/route.ts
@app/api/superadmin/webhooks/replay/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar pagina e componente de listagem de webhook events</name>
  <files>app/superadmin/(dashboard)/webhooks/page.tsx, app/superadmin/(dashboard)/webhooks/components/webhook-events-list.tsx, tests/unit/stripe/webhook-log-ui.test.tsx</files>
  <read_first>
    - app/superadmin/(dashboard)/faturas/page.tsx
    - app/superadmin/(dashboard)/faturas/components/invoice-list.tsx
    - app/shared/components/ui/table.tsx
    - app/shared/components/ui/button.tsx
  </read_first>
  <behavior>
    - Teste: renderiza colunas `Evento`, `Status`, `Recebido em`, `Duracao`, `Acao`.
    - Teste: exibe estado vazio quando API retorna lista vazia.
    - Teste: exibe badge visual para `processing`, `processed`, `failed`.
  </behavior>
  <action>Criar pagina server `webhooks/page.tsx` com cabecalho "Webhooks" e descricao operacional. Implementar componente client `webhook-events-list.tsx` com fetch para `/api/superadmin/webhooks`, filtro por status, paginacao por `starting_after`, render de payload resumido (event_type + stripe_event_id + erro resumido).</action>
  <verify>
    <automated>npx jest tests/unit/stripe/webhook-log-ui.test.tsx --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `webhook-events-list.tsx` contem chamada GET para `/api/superadmin/webhooks`.
    - Componente renderiza explicitamente os tres status (`processing`, `processed`, `failed`).
    - Teste de UI cobre estado vazio e tabela com eventos.
  </acceptance_criteria>
  <done>Tela de log de webhook renderiza corretamente e passa nos testes de UI.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar acao de replay e navegacao no sidebar</name>
  <files>app/superadmin/(dashboard)/webhooks/components/webhook-events-list.tsx, app/superadmin/(dashboard)/components/superadmin-sidebar.tsx</files>
  <read_first>
    - app/superadmin/(dashboard)/components/superadmin-sidebar.tsx
    - app/superadmin/(dashboard)/assinaturas/components/subscription-list.tsx
    - app/api/superadmin/webhooks/replay/route.ts
  </read_first>
  <action>Adicionar botao "Reprocessar" apenas para status `failed`, disparando `POST /api/superadmin/webhooks/replay` com `{ eventId }`, com feedback visual (loading/sucesso/erro). Incluir item de menu "Webhooks" no sidebar apontando para `/superadmin/webhooks` e preservar padrao visual existente.</action>
  <verify>
    <automated>npx jest tests/unit/stripe/webhook-log-ui.test.tsx --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar contem link para `/superadmin/webhooks` com rotulo "Webhooks".
    - Botao de replay aparece somente para linhas com status `failed`.
    - Clique no replay chama endpoint `/api/superadmin/webhooks/replay` e atualiza lista apos sucesso.
  </acceptance_criteria>
  <done>Operador superadmin consegue navegar ate Webhooks e reprocessar eventos falhos pela UI.</done>
</task>

</tasks>

<verification>
Executar `npx jest tests/unit/stripe/webhook-log-ui.test.tsx --no-coverage` e validar rota `/superadmin/webhooks` carregando com dados mockados.
</verification>

<success_criteria>

- Superadmin enxerga historico recente de webhooks com status e payload resumido.
- Reprocessamento de falhas pode ser acionado pela interface.
- Pagina aparece no menu oficial do dashboard.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-stripe-integration-testing-observability/02-02-SUMMARY.md`
</output>
