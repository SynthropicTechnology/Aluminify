# Deferred Items

## 2026-03-24 - Plan 01-02

- `npm run check:quick` fails due pre-existing lint errors in `.claude/worktrees/**` (unrelated worktree files outside plan scope).
- Decision: deferred per GSD scope boundary; not fixed in this plan because changes are unrelated to Task 1/Task 2 implementation.

## 2026-03-24 - Plan 01-03

- `npm run check:quick` still fails due pre-existing lint/type issues under `.claude/worktrees/**` from parallel worktree test files.
- Decision: deferred per GSD scope boundary; no fixes applied because these files are unrelated to billing route hardening changes in Plan 01-03.
